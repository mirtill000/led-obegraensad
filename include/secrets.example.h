#pragma once

// Copy this file to include/secrets.h and fill in your WiFi network.
// secrets.h is git-ignored so your password never ends up in the repo.
// The firmware doesn't build without it.
#define WIFI_SSID "your-network"
#define WIFI_PASSWORD "your-password"
